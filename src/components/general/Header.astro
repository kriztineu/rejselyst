---
import Title from "../../pages/destinationer/[title].astro";
import Destination from "../destinationer/destination.astro";
import BTNPrimary from "./BTNPrimary.astro";

const data = await fetch("https://zywerzwialywqwbenhax.supabase.co/rest/v1/destinations", {
  method: "GET",
  headers: {
    apikey: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Inp5d2VyendpYWx5d3F3YmVuaGF4Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3MTY1MzY1NTYsImV4cCI6MjAzMjExMjU1Nn0.qAjwQtmmuFvZc9VU5aDtI4V2bkN4XbsLcKKjq-xiPcM",
  },
}).then((res) => res.json())

console.log(data)
---

<header>
    <div class="menu-container">
        <div class="main-menu">
          <div class="logoContainer">
              <li><a href="/index.html"><img src="/svg/rejselyst..svg" class="logo" alt="Rejselyst logo"/></a></li>
              <div class="menu-left">
                <li><a class="menuBTN link-text-small" href="/rejseforslag.html"> REJSEFORSLAG</a></li>
                <div class="dropdown">
                  <button class="dropbtn">
                    <li><a class="menuBTN link-text-small" href="/destinationer.html"> DESTINATIONER</a></li>
                  </button>
                  <div class="dropdown-content">
                    <a href={`${data.title='Maldiverne'}`}>MALDIVERNE</a>
                    <a href={`${data.title='Grækenland'}`}>GRÆKENLAND</a>
                    <a href={`${data.title='Seychellerne'}`}>SEYCHELLERNE</a>
                  </div>
                </div>
              </div>

          </div>

            <div class="icons">
              <ul>
                <li><a class="menuBTN" href="/kontaktos.html"><BTNPrimary data="KONTAKT OS"/></a></li>
                <li class="burger-menu"><img src="/svg/burgermenu.svg" alt="Burger-Menu" /></li>
              </ul>
            </div>
          </div>
    </div>

    <div class="overlaymenu-container">
        <div class="icons-close">
          <div class="overlayLogo">
            <li><a href="/index.html"><img src="/svg/rejselyst.sand.svg" class="logo" alt="Rejselyst logo"/></a></li>
            <li class="burger-menu2"><img src="/svg/x.svg" alt="Burger-Menu2" /></li>
          </div>
          <ul class="ulicons">
             
                <div class="storMenu">
                    <li><a class="h1-display-small" href="/rejseforslag.html">Rejseforslag</a></li>
                    <li><a class="h1-display-small" href="/omos.html">Om os</a></li>
                    <li><a class="h1-display-small" href="/destinationer.html">Destinationer</a></li>
                    <li><a class="h1-display-small" href="/kontaktos.html">Kontakt os</a></li>
              </div>
          </ul>
          </div>
    </div>

</header>

<style>

.navbar {
  overflow: hidden;
  background-color: none;
}

/* Links inside the navbar */
.navbar a {
  float: left;
  text-align: center;
  padding: 14px 16px;
  text-decoration: none;
}

/* The dropdown container */
.dropdown {
  float: left;
  overflow: hidden;
}

/* Dropdown button */
.dropdown .dropbtn {
  font-size: 16px;
  border: none;
  outline: none;
  background-color: inherit;
  font-family: inherit; /* Important for vertical align on mobile phones */
  margin: 0; /* Important for vertical align on mobile phones */
}

/* Add a red background color to navbar links on hover */
.navbar a:hover, .dropdown:hover .dropbtn {
  /* background-color: red; */
}

/* Dropdown content (hidden by default) */
.dropdown-content {
  display: none;
  position: absolute;
  min-width: 160px;
  z-index: 1;
  padding-top: 10px;
}

/* Links inside the dropdown */
.dropdown-content a {
  float: none;
  padding: 12px 16px;
  padding-left: 5px;
  text-decoration: none;
  display: block;
  text-align: left;
  font-family: "Be Vietnam Pro";
  font-size: 15px;
  font-style: normal;
  font-weight: 500;
  line-height: 20px; /* 294.118% */
}

/* Add a grey background color to dropdown links on hover */
.dropdown-content a:hover {
  background-color: var(--yellow100);
}

/* Show the dropdown menu on hover */
.dropdown:hover .dropdown-content {
  display: block;
}

  .overlayLogo {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
  }
.ulicons, a {
  text-decoration: none;
}

  .menu-left, li {
    gap: var(--space-vertical-gap-small);
    align-self: left;
  }

  .menu-left {
    padding-left: var(--space-vertical-gap-medium);
  }

.logoContainer {
  display: flex;
  gap: var(--space-vertical-gap-medium);
  justify-content: center;
  
}
.main-menu {
  display: flex;
  justify-content: space-between;

}

  .menu-container {
      /* position: absolute; */
      z-index: 10000;
      width: 100%;
  }

  /* .overlayLogo a {
    position: absolute;
  } */
    header{
    list-style-type: none;
    z-index: 1000;
  }

  body.menu-open .menu-container {
      display: none;
  }
  .icons ul {
    display: flex;
    list-style-type: none; 
  }
  .burger-menu img, .burger-menu2 img {
      width: 40px;
      height: 40px;
  }

  .burger-menu,
  .burger-menu2 {
    cursor: pointer;
    transition: 0.5s ease;
  }

  .burger-menu:hover, .burger-menu2:hover {
    opacity: 0.7;
    transition: 0.5s ease;
  }

  .logo:hover {
    opacity: 0.7;
    transition: 0.5s ease;
  }

  body .open {
    transform: translateX(0);
  }

  .overlaymenu-container {
    height: 100vh;
    width: 100vw;
    background-color: var(--surface-brand-contrast);
    position: fixed;
    top: 0;
    right: 0;
    overflow: hidden;
    transform: translateX(100%);
    transition: transform 0.2s;
    z-index: 99;
  }

  .burger-menu2 {
    display: flex;
    justify-content: right;
    text-align: right;
    z-index: 2000;
  }

  .storMenu {
    display: flex;
    justify-content: left;
    flex-direction: column;
    gap: var(--space-horisontal-margin-xsmall);
    padding: 3rem;
  }

  .storMenu a {
    color: var(--surface-secondary);
  }

  .icons-close ul {
    display: flex;
    flex-direction: column;
    list-style-type: none;
    z-index: 1000;
  }

  .storMenu a:hover {
    text-decoration: underline;
    cursor: pointer;
  }

  .icons-close a {
    display: block;
    z-index: 1000;
  }

  .menuBTN {
    display: none;
    text-decoration: none;
  }

  .menu-container, .overlaymenu-container {
    /* padding: 0px 60px; */
    padding: var(--space-horisontal-margin-xsmall, 40px)
  }


    @media screen and (min-width: 800px) {


      .menu-left {
        display: flex;
      }

      .menu-left a:hover {
        text-decoration: underline;
      }

      .menu-left a {
        color: var(--components-button-primary-background-default);
      }

      .icons {
        display: flex;
      }
      header {
      z-index: 1000;
      display: flex;
      gap: var(--space-vertical-gap-large);
      }

      .menuBTN {
      display: block;
    }

    .icons ul {
      gap: var(--space-vertical-gap-small);
    }

    .icons-close a {
    display: block;
    z-index: 1000;
  }

  }

</style>

<script>
  const overlaymenu = document.querySelector(".overlaymenu-container");
  const burgermenu = document.querySelector(".burger-menu");
  const close = document.querySelector(".burger-menu2");
  const close2 = document.querySelector("section");

  const some = document.querySelector(".SoMe")

  burgermenu.addEventListener("click", function () {
    overlaymenu.classList.add("open");
    document.body.classList.add("menu-open"); 
    some.classList.toggle("blue")
  });

  close.addEventListener("click", function () {
    overlaymenu.classList.remove("open");
    document.body.classList.remove("menu-open"); 
    some.classList.toggle("blue")

  });

  close2.addEventListener("click", function () {
    overlaymenu.classList.remove("open");
    document.body.classList.remove("menu-open");
  });

  overlaymenu.addEventListener("click", function () {
    overlaymenu.classList.remove("open");
    document.body.classList.remove("menu-open"); // Add this line
  });
</script>